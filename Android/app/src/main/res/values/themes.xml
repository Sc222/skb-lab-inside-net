<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.InsideNet" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/theme_light_primary</item>
        <item name="colorPrimaryVariant">@color/theme_light_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/theme_light_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/theme_light_primary_variant</item>
    </style>

    <style name="Theme.InsideNet.WhiteStatusBar" parent="Theme.InsideNet">
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Toolbar.Icons.Primary.Overlay" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- This attributes is used by navigation icon and overflow icon -->
        <item name="colorOnPrimary">?attr/colorPrimary</item>
    </style>

    <style name="Toolbar.Surface.Primary" parent="Widget.MaterialComponents.Toolbar.Surface">
        <item name="titleTextColor" tools:targetApi="m">?attr/colorPrimary</item>
        <item name="android:textColorSecondary">?attr/colorPrimary</item>
        <item name="android:colorControlNormal" tools:targetApi="lollipop">?attr/colorPrimary</item>
    </style>

    <style name="LargeAccentButtonStyle" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="materialThemeOverlay">@style/AccentButtonThemeOverlay</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="cornerRadius">9dp</item>
    </style>

    <style name="AccentButtonThemeOverlay">
        <item name="colorPrimary">@color/theme_light_primary</item>
    </style>
</resources>